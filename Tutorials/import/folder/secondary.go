package secondary //all files in folder "folder" must be of package secondary

import "fmt"

//Add Comment Required
func Add(x, y int) int { //must be capitalized
	fmt.Println("In add")
	return x + y
}
